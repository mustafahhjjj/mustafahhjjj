---
deployment:
  tasks:
    - export DEPLOYPATH=/home/u1878454/public_html/
    - /bin/mkdir -p "$DEPLOYPATH"
    - 'if [ "$(pwd -P)/" != "$(cd "$DEPLOYPATH" && pwd -P)/" ]; then /bin/cp -f ./*.html "$DEPLOYPATH"; fi'
    - 'if [ "$(pwd -P)/" != "$(cd "$DEPLOYPATH" && pwd -P)/" ]; then /bin/cp -Rf ./css ./js ./dersler ./siniflar ./testler "$DEPLOYPATH"; fi'
